#!/usr/bin/env node
import { getArgs } from "./helpers/arg.js";
const initCLI = () => {
  const args = getArgs(process.argv);
  console.log(args);
  if (args.h) {
  }
  if (args.s) {
  }
  if (args.t) {
  }
};
initCLI();
